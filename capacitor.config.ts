import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.walletsmlt.mobile',
  appName: 'wallet-smlt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
